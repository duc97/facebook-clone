# TEMPLATE ONLY — DO NOT COMMIT REAL VALUES
# Use: kubeseal --fetch-cert | kubeseal --format=yaml > sealed-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: facebook-clone-secrets
  namespace: production
  annotations:
    # Replace with SealedSecret in production
    note: "Template only — use sealed-secrets or external-secrets-operator"
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://fb:CHANGE_ME@postgres-service:5432/facebook_clone"
  REDIS_URL: "redis://redis-service:6379/0"
  JWT_SECRET_KEY: "CHANGE_ME_USE_LONG_RANDOM_STRING"
  S3_BUCKET_NAME: "facebook-clone-media"
  S3_ENDPOINT_URL: ""
